# Hello world React

This is my first<b> React </b>project — an animated <b>Hello World 🌍<b>

## 📦 Technologies

Built with:

- `HTML:` Markup language for creating the structure.
- `CSS:` For styling and animations.
- `javaScript:` Adds interactivity and dynamic behavior to a webpage.
- `TailwindCSS:` A utility-first CSS framework that helps you style websites faster using pre-built classes.


## ⚙️ Getting Started

To view the project:

1. Download the project files.
2. Open VS Code, then open the terminal and run `npm install` and `npm run dev` :
3. Open the project in your browser — Vite will show you a local URL (like http://localhost:5173).

## 👉 Demo
<video src="src\assets\hello.mp4" width="500" autoplay loop muted playsinline> </video>
